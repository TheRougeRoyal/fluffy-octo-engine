{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfile": "Dockerfile"
  },
  "deploy": {
    "startCommand": "dotnet TradingEngine.dll",
    "healthCheckPath": "/health",
    "restartPolicyType": "ALWAYS"
  }
}
